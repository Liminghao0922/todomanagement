info:
  name: Create Owner
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:7071/api/owners
  body:
    type: json
    data: |-
      {
        "name": "MOD Administrator",
        "email": "admin@xxx.onmicrosoft.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
